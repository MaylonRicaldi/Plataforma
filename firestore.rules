rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Deny all client-side access by default
    // All reads/writes go through the backend (Flask) using Admin SDK
    match /{document=**} {
      allow read, write: if false;
    }

  }
}
